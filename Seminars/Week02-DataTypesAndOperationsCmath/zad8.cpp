#include <iostream>
#include <cmath>

int main ()
{
    double a, b, c;

    a - b + pow(b,2) * pow(a,3) - pow(c,3); // a)
    a * sqrt(b) - sqrt(pow(a,3)) * sqrt(c); // b)
    fabs(pow(c,3) - pow(a,3) * pow(b,2)) + pow(a,2); // v)
    cos(M_PI/6) * c + sin(M_PI/4) * log10(a); // g)
    sqrt(pow(a,3)) * pow(c,3) + cos(M_PI/4) * pow(a,3); // d)
    
    return 0;
}