#include <iostream>

int main ()
{
    int length, width;

    std::cin >> length >> width;

    std::cout << (length + width) * 2 << std::endl;
    
    std::cout << length * width << std::endl;

    return 0;
}

